mixin icon(name,mod)
   if(mod)
      svg(class="icon-" + name + ' ' + mod)
         use(xlink:href="images/sprite/sprite.svg#" + name)
   else
      svg(class="icon-" + name)
         use(xlink:href="images/sprite/sprite.svg#" + name)
